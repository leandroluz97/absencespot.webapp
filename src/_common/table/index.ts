import { TableColumn, TableColumnHead, TableContent, TableRoot, TableRow } from './Table';

export const Table = {
    Root: TableRoot,
    Content: TableContent,
    Row: TableRow,
    Column: TableColumn,
    ColumnHead: TableColumnHead,
};
