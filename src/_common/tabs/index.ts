import { BasicTabsLink, BasicTabsList, BasicTabsRoot, BasicTabsTrigger } from './BasicTabs';

export const BasicTabs = {
    Root: BasicTabsRoot,
    List: BasicTabsList,
    Trigger: BasicTabsTrigger,
    Link: BasicTabsLink,
};
